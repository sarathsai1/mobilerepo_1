// Main theme file that aggregates all theme-related settings
import { colors } from './colors';
import { fonts } from './fonts';

export const theme = {
  colors,
  fonts,
  // You can include other theme-related settings such as spacing, border radii, etc.
};
